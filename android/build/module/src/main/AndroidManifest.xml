<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
			<uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
			<uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
			<uses-permission android:name="android.permission.INTERNET"/>
			<uses-permission android:name="android.permission.WAKE_LOCK"/>
			<application>
				<receiver android:name="ti.audiostream.MediaActionReceiver" android:exported="true">
					<intent-filter>
						<action android:name="android.intent.action.MEDIA_BUTTON"/>
						<action android:name="ti.audiostream.PLAY"/>
						<action android:name="ti.audiostream.PAUSE"/>
						<action android:name="ti.audiostream.PLAY_PAUSE"/>
						<action android:name="ti.audiostream.STOP"/>
						<action android:name="ti.audiostream.NEXT"/>
						<action android:name="ti.audiostream.PREV"/>
					</intent-filter>
				</receiver>
				<service android:name="ti.audiostream.MediaPlaybackService" android:foregroundServiceType="mediaPlayback" android:stopWithTask="false" android:exported="false"/>
			</application>
		</manifest>
